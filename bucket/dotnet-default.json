{
    "version": "10.0.401",
    "description": "Microsoft .NET Framework 10.0.100 Developer Pack",
    "homepage": "https://dotnet.microsoft.com/zh-cn/download/dotnet/10.0",
    "license": "MIT",
    "notes": [
        "This manifest will not affect the installation of other versions and is compatible with dotnet-scoop.",
        "You can now remove this installer with 'scoop uninstall -p dotnet-default'."
    ],
    "architecture": {
        "64bit": {
            "url": "https://builds.dotnet.microsoft.com/dotnet/Sdk/10.0.401/dotnet-sdk-10.0.401-win-x64.zip",
            "hash": "sha512:24b670ad3d923bfcf47df6c3b034152398b42f6dbc388e10d783aee1cfb5e5817d399fc0ae2a12cfa822a55e61d34830ccb15c50ef6efee437ab874bb7c79430"
        }
    },
    "post_install": [
        "$runtimepath = \"$env:ProgramFiles\\dotnet\\shared\"",
        "$sdkpath = \"$env:ProgramFiles\\dotnet\\sdk\"",
        "if (!(Test-Path $runtimepath)) {",
        "    New-Item -ItemType Directory -Path $runtimepath -Force | Out-Null",
        "}",
        "if (!(Test-Path $sdkpath)) {",
        "    New-Item -ItemType Directory -Path $sdkpath -Force | Out-Null",
        "}",
        "Get-ChildItem -Path \"$dir\\shared\" | Move-Item -Destination $runtimepath -Force 2>&1 | Out-Null",
        "Get-ChildItem -Path \"$dir\\sdk\" | Move-Item -Destination $sdkpath -Force 2>&1 | Out-Null"
    ],
    "checkver": {
        "url": "https://builds.dotnet.microsoft.com/dotnet/release-metadata/releases-index.json",
        "jsonpath": "$..releases-index[?(@.support-phase == 'active')].latest-sdk",
        "regex": "([\\d.]+)"
    },
    "autoupdate": {
        "architecture": {
            "64bit": {
                "url": "https://builds.dotnet.microsoft.com/dotnet/Sdk/$version/dotnet-sdk-$version-win-x64.zip"
            }
        },
        "hash": {
            "url": "https://builds.dotnet.microsoft.com/dotnet/release-metadata/$majorVersion.$minorVersion/releases.json",
            "regex": "(?s)$basename.*?$sha512"
        }
    }
}
